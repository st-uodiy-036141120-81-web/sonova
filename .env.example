VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Production site (Cloudflare Workers)
# SITE_URL=https://sonova.st-uodiy-036141120-81.workers.dev

# Cloudflare CDN (optional) — prefix for public media URLs
VITE_CDN_BASE_URL=https://cdn.yourdomain.com

# Cloudflare R2 upload worker URL (optional)
# Deploy supabase/r2-worker.js to Cloudflare Workers
VITE_R2_UPLOAD_URL=https://your-worker.workers.dev/upload

# Web Push (optional) — generate with: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=
